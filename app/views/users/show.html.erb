<%= "#{@user.name},#{@user.email}" %>
<% if logged_in? %>
	<%= render "shared/micropost_form" %>
<% end %>
    <% if @user.microposts.any? %>
      <ol class="microposts">
        <%= render @microposts %>
      </ol>
    <% end %>

